export const SectionHeading = ({ title }: { title: string }) => {


    return (
        <div className="w-full text-white font-sans text-xl text-start font-bold">
            {title}
        </div>
    );

}